# Deprecated compatibility path

The public evaluation guide is now [ACTIVEARENA_EVAL_LAUNCH.md](ACTIVEARENA_EVAL_LAUNCH.md).
It uses the frozen `activearena-sim` and `activearena-vla` environments and the
`ACTIVEARENA_*` variables. The historical filename remains only so old links do
not break.
